<div class="row">
  <div class="col-md-12">
    <h1>Inbox</h1>
  </div>

  <p>{{#link-to 'new-email' classNames="btn btn-default"}}Compose Email{{/link-to}}</p>

  <ul>
    {{#each email in model}}
      <li>{{#link-to 'email' email}}{{email.sender}} {{email.subject}}{{/link-to}}</li>
    {{/each}}
  </ul>
</div>
